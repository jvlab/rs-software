__pycache__
./*/__pycache__
.DS_Store
src/rs_py/.idea
src/rs_py/samples/outputs
.idea
src/tests/*.fig
src/tests/*.mat
svd_issue
src/benchmarks/*.zip
junk*.*
*.ppt*
*.doc*
*.asv
site/
*__pycache__*
# Function pages are generated on every build. The demo pages under
# docs/mfiles/demos are NOT: they are rendered when a demo is captured locally
# and committed, because the documentation build has no MATLAB.
docs/mfiles/*.md
*.pyc

# capture scratch: specs and manifests of the last local run
build/

# a perceptual_space_geometry checkout placed inside the repository;
# run_all.m picks it up from here
perceptual_space_geometry


